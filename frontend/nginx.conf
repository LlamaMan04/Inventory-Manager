server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Required for React BrowserRouter routes on direct navigation or refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
