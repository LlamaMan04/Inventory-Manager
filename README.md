A simple inventory management application designed to be self-hosted. The database and backend api will be hosted on a server, and the frontend React app will be used to interface with it. 

The first project that I've done with React and Node.js. 
